#Given a number n find the longest run of consecutive equal digits in n using only a loop, division and modulo(e.g 1122333145 => longesr run is "333", length 3). Print both the
#digit and run the length.

number_n = 1122333145
for number in number_n
    if(number.length % )
    print
